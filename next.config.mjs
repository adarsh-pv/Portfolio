/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      unoptimized: true, // Only if using static hosting
    },
  };
  
  export default nextConfig;
// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     output: 'export',  // Enables static export
//     images: {
//       unoptimized: true, 
//     },
//     basePath: '/Portfolio',  
//   };
  
//   export default nextConfig;
  